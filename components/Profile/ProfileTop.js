import React from 'react';
import { View } from 'react-native';
import { colors } from '../../styles';
function ProfileTop() {
	return <View style={{ backgroundColor: colors.mainColor, paddingVertical: 60 }} />;
}

export default ProfileTop;
